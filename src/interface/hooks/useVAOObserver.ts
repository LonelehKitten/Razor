import ResourceLoader from "@engine/core/ResourceLoader";
import { useDispatch, useSelector } from "@store/Root.store";
import { useEffect, useMemo } from "react";



function useVAOObserver(): string[] {

  //const vaoObserver = useSelector(state => state.razor.resources[ERazorResources.VAO]);

  return []//vaoObserver.keys

}

export default useVAOObserver;